import { useState } from "react";
import { ipcRenderer } from "electron";

export default function Home() {
  return (
    <div className="h-screen flex items-center justify-center bg-gradient-to-r from-indigo-500 to-purple-500"></div>
  );
}
